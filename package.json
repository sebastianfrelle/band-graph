{
  "name": "band-graph",
  "version": "0.0.1",
  "scripts": {
    "build": "babel src -d lib",
    "test": "mocha --compilers js:babel/register"
  },
  "description": "Represent bands (and potentially other things) as a directed graph",
  "main": "index.js",
  "repository": "https://github.com/sebastianfrelle/band-graph.git",
  "author": "sebastian.frelle@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "mocha": "^4.0.1"
  }
}
